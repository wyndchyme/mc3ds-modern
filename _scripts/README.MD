## Script Listing
```
/reitterateHashListAndVersionNumber.py
    - Takes in a HashList and itterates over texture version (updating texture versioning for Application). 
    *do NOT delete hashlist.sha256

/generateBaseHashList.py
    - Generates a hashList for each file, uses appConfiguration.info.
    *do NOT delete hashlist.sha256

/getAllFileList.py
    - gets all files and lists them into appConfiguration.info

/addNumbersToAppConf.py
    - adds numbers to App Config in order to allow textures to be updated.
```